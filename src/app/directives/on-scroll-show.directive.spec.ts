import { OnScrollShowDirective } from './on-scroll-show.directive';

describe('OnScrollShowDirective', () => {
  it('should create an instance', () => {
    const directive = new OnScrollShowDirective();
    expect(directive).toBeTruthy();
  });
});
